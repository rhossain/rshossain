import { BlockToHtmlPipe } from './block-to-html.pipe';

describe('BlockToHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockToHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
